import { create } from 'zustand';

export interface iGlobal {
    // sa: WalletInstance | null;
    // setConnectedWallet: (wallet: WalletInstance | null) => void;
}
export const useGlobalStore = create<iGlobal>((set) => ({
   
}));
